services:
  toms-diary-backend:
    build: .
    container_name: toms-diary-backend
    ports:
      - "18080:8080"
    environment:
      - VLLM_HOST=http://localhost:8001/v1
      - VLLM_MODEL=default
    restart: unless-stopped
